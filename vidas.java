import greenfoot.*;  // (World, Actor, GreenfootImage, Greenfoot and MouseInfo)

/**
 * Write a description of class vidas here.
 * 
 * @author (your name) 
 * @version (a version number or a date)
 */
public class vidas extends Actor
{
    /*
    private int val = 3;
    private int tar = 0;
    private String text;
    private int stringLength;
    //private NivelUno n = new NivelUno();
    
    public vidas(String prefix, int vidas){
        text = prefix;
        stringLength = (text.length() + 2) * 16;
        
        setImage(new GreenfootImage(stringLength,24));
        GreenfootImage image = getImage();
        Font font = image.getFont();
        image.setFont(font.deriveFont(24.0F));
        image.setColor(Color.WHITE);
        
        updateImage(vidas);
    }
    public void act()
    {
        // Add your action code here.
        // Add your action code here.
        /*if(val < tar){
            val++;
            updateImage();
        }else if(val > tar){
            val--;
            
        }
        updateImage(n.vid);
    }
    
    public void add(int v){
        tar += v;
    }
    
    public void substract(int v){
        tar -= v;
    }
    
    public int getValue(){
        return val;
    }
    private void updateImage(int vidas){
        GreenfootImage image = getImage();
        image.clear();
        image.drawString(text + vidas,1,18);
    }
    */
    
}
